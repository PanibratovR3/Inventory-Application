<!DOCTYPE html>
<html>
    <head></head>
    <body>
        <h1><%= title %></h1>
        <ul>
            <% if (locals.errors) { %>
                <% errors.forEach((error) => { %>
                    <li><%= error.msg %></li>
                <% }) %>
            <% }; %>
        </ul>
        <form action="/genres/create" method="POST">
            <label for="name">Name:</label>
            <input type="text" name="genreName" id="name" required />
            <button type="submit">Add</button>
        </form>
    </body>
</html>