<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <link rel="stylesheet" href="/style.css" />
        <title>Add new publisher</title>
    </head>
    <body>
        <%- include("subviews/header") %>
        <div class="main">
            <%- include("subviews/navigation") %>
            <div>
                <div class="subheader"><%= title %></div>
                <ul>
                    <% if (locals.errors) { %>
                        <% errors.forEach((error) => { %>
                            <li class="error"><%= error.msg %></li>
                        <% }); %>    
                    <% } %>
                </ul>
                <form action="/publishers/create" method="post">
                    <div class="create-form-row">
                        <label for="name">Name:</label>
                        <input type="text" name="publisherName" id="name" required maxlength="50" />
                    </div>
                    <div class="create-form-row">
                        <button type="submit" class="add-button">Add</button>
                    </div>
                </form>
            </div>
        </div>
    </body>
</html>