<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <link rel="stylesheet" href="/style.css" />
        <title>Update genre</title>
    </head>
    <body>
        <%- include("subviews/header") %>
        <div class="main">
            <%- include("subviews/navigation") %>
            <div>
                <div class="subheader"><%= title %></div>
                <ul>
                    <% if (locals.errors) { %>
                        <% errors.forEach((error) => { %>
                            <li class="error"><%= error.msg %></li>
                        <% }); %>
                    <% }; %>
                </ul>
                <form action="/genres/<%= genre.id %>/update" method="POST">
                    <div class="update-form-row">
                        <label for="name">Name:</label>
                        <input type="text" name="genreName" id="name" value="<%= genre.name %>" required />
                    </div>
                    <div class="update-form-row">
                        <button type="submit" class="update-button">Update</button>
                    </div>
                </form>
            </div>
        </div>
    </body>
</html>